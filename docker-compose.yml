services:
  db:
    image: postgres:16-alpine
    container_name: minority_game_db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: minority_game
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
